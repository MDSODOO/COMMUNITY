# Baseline de configuración de Asistencias / Attendance configuration baseline

Este documento tiene dos partes / This document has two parts:

1. **Baseline** (entre los marcadores de abajo): estado real de Ajustes >
   Asistencias, generado por `e2e_tests/audit_attendance_config.spec.ts`.
   Todavía no se ha ejecutado contra la instancia — ver nota de permisos
   más abajo. / **Baseline** (between the markers below): the real state of
   Settings > Attendances, generated by
   `e2e_tests/audit_attendance_config.spec.ts`. Not yet run against the
   instance — see the permissions note below.
2. **Arquitectura del módulo `hr_attendance_ip_autologin`** (después de los
   marcadores): diseño original del check-in por IP (implementado, commit
   `564e23b`) más el checkout dinámico agregado despues (logout explícito +
   cron de sesión abandonada, también implementado en ese mismo commit). Los
   fragmentos de código de abajo son el diseño original tal como se propuso
   -- el código real en `custom_addons/hr_attendance_ip_autologin/` difiere
   en algunos detalles encontrados durante la implementación (ver notas
   inline). / **Architecture for the `hr_attendance_ip_autologin` module**
   (after the markers): the original IP check-in design (implemented,
   commit `564e23b`) plus the dynamic checkout added afterward (explicit
   logout + abandoned-session cron, also implemented in that same commit).
   The code snippets below are the original proposal as first written -- the
   real code in `custom_addons/hr_attendance_ip_autologin/` differs in a
   few details found during implementation (see inline notes).

<!-- BASELINE:START (auto-generado, no editar a mano / auto-generated, do not hand-edit) -->

**Generado / Generated:** 2026-07-28T17:38:27.542Z

**Instancia / Instance:** http://localhost:8069 (DB: medicinedepot_dev)

| Campo / Field | Descripcion / Description | Valor actual / Current value |
|---|---|---|
| `attendance_kiosk_mode` | Modo Quiosco / Kiosk Mode | Código de barras / RFID y selección manual |
| `attendance_from_systray` | Asistencias desde el Backend / Attendances from Backend | false |
| `auto_check_out` | Salida automatica / Automatic Check-Out | false |
| `auto_check_out_tolerance` | Tolerancia salida automatica (hrs) / Auto check-out tolerance (hrs) | _(campo condicional, no visible con la config. actual / conditional field, not visible with current config)_ |
| `absence_management` | Gestion de ausencias / Absence Management | false |
| `attendance_device_tracking` | Rastreo de dispositivo/ubicacion / Device & Location Tracking | false |
| `attendance_barcode_source` | Fuente de codigo de barras (Kiosco) / Barcode source (Kiosk) | Cámara frontal |
| `attendance_kiosk_delay` | Tiempo de mensaje (seg) / Display time (sec) | 10 |
| `attendance_kiosk_use_pin` | Usar PIN en Quiosco / Use PIN in Kiosk | false |
| `hr_attendance_display_overtime` | Mostrar horas extra / Display Extra Hours | false |
| `attendance_overtime_validation` | Validacion de horas extra / Extra Hours Validation | (ninguno seleccionado / none selected) |

Captura / Screenshot: `screenshots/attendance_settings_baseline.png`

<!-- BASELINE:END -->

## Arquitectura: `hr_attendance_ip_autologin` (implementada) / Architecture (implemented)

### Objetivo / Goal

Cuando un empleado inicia sesión en Odoo desde una IP que coincide con la de
su sucursal (registrada de antemano), registrar su check-in automáticamente
sin que tenga que pasar por el Quiosco ni marcar manualmente. / When an
employee logs into Odoo from an IP that matches their branch's registered
IP, automatically log their check-in without going through the Kiosk or
marking manually.

### Contexto relevante del negocio / Relevant business context

Esta instancia usa **una `res.company` por sucursal** (Cancún, Campeche,
Chetumal, Mérida, Playa del Carmen, Ticul — verificado en `res_company` /
`stock_warehouse`, 1 almacén por compañía). Por eso "sucursal" mapea
directo a `res.company`, no a `stock.warehouse` ni a un campo nuevo. /
This instance uses **one `res.company` per branch** (Cancún, Campeche,
Chetumal, Mérida, Playa del Carmen, Ticul — verified in `res_company` /
`stock_warehouse`, 1 warehouse per company). So "branch" maps directly to
`res.company`, not to `stock.warehouse` or a new field.

`proxy_mode = True` ya está activo en `config/odoo.conf` /
`config/odoo_test.conf`. Esto importa porque la instancia corre detrás de un
reverse proxy: con `proxy_mode` activo, Werkzeug reescribe
`request.httprequest.remote_addr` a partir de `X-Forwarded-For`, así que el
diseño de abajo puede confiar en ese valor sin lógica extra de
des-proxificación. Si `proxy_mode` estuviera apagado, `remote_addr` sería
la IP del proxy para *todas* las conexiones y el matching nunca
funcionaría — es la primera cosa a verificar en cualquier entorno donde se
instale este módulo. / `proxy_mode = True` is already on in
`config/odoo.conf` / `config/odoo_test.conf`. This matters because the
instance runs behind a reverse proxy: with `proxy_mode` on, Werkzeug
rewrites `request.httprequest.remote_addr` from `X-Forwarded-For`, so the
design below can trust that value without extra de-proxying logic. If
`proxy_mode` were off, `remote_addr` would be the proxy's IP for *every*
connection and the matching would never work — this is the first thing to
verify in any environment where this module gets installed.

**Nota de terminología (regla del proyecto) / Terminology note (project
rule):** este módulo es de RRHH y no toca inventario directamente. La única
sección donde surge una analogía con almacén es la nota de más abajo sobre
extender el mismo patrón de IP a operaciones de almacén — ahí, y en
cualquier parte donde se hable de cantidades de producto, el término
correcto es siempre **"A la mano"**. Nunca "Disponible", "Stock" ni
"Existencias". / **Terminology note (project rule):** this module is HR and
doesn't touch inventory directly. The one place a warehouse analogy comes
up is the note below about extending the same IP pattern to warehouse
operations — there, and anywhere product quantities are discussed, the
correct term is always **"On Hand"** (Spanish: **"A la mano"**). Never
"Available", "Stock", or "Existencias".

### Modelo 1: `hr.attendance.authorized.ip` (nuevo) / Model 1 (new)

```python
# custom_addons/hr_attendance_ip_autologin/models/hr_attendance_authorized_ip.py
import ipaddress

from odoo import api, fields, models
from odoo.exceptions import ValidationError


class HrAttendanceAuthorizedIp(models.Model):
    _name = "hr.attendance.authorized.ip"
    _description = "IP autorizada para check-in automatico por sucursal / Authorized IP for branch auto check-in"

    name = fields.Char(required=True, help="Ej. 'Router principal - MDS Cancun'")
    company_id = fields.Many2one(
        "res.company", required=True, index=True, default=lambda self: self.env.company,
        help="Sucursal a la que pertenece esta IP (1 res.company = 1 sucursal en esta instancia).",
    )
    ip_address = fields.Char(
        required=True,
        help="IP unica (189.203.10.5) o rango CIDR (189.203.10.0/24).",
    )
    active = fields.Boolean(default=True)

    @api.constrains("ip_address")
    def _check_ip_address(self):
        for rec in self:
            try:
                ipaddress.ip_network(rec.ip_address, strict=False)
            except ValueError as exc:
                raise ValidationError(
                    f"'{rec.ip_address}' no es una IP ni un rango CIDR valido."
                ) from exc

    def _matches(self, remote_ip):
        """True si remote_ip cae dentro de este registro (IP unica o CIDR)."""
        try:
            network = ipaddress.ip_network(self.ip_address, strict=False)
            return ipaddress.ip_address(remote_ip) in network
        except ValueError:
            return False
```

### Modelo 2: extensión de `res.company` / Model 2: `res.company` extension

```python
# custom_addons/hr_attendance_ip_autologin/models/res_company.py
from odoo import fields, models


class ResCompany(models.Model):
    _inherit = "res.company"

    attendance_authorized_ip_ids = fields.One2many(
        "hr.attendance.authorized.ip", "company_id", string="IPs autorizadas de asistencia / Authorized attendance IPs",
    )
```

### Punto de intercepción del login / Login interception point

Sobrescribir `res.users.authenticate` es frágil: en Odoo 19 hay múltiples
rutas de autenticación (password, OAuth/SSO, 2FA, API keys) y no todas pasan
por el mismo método. El punto estable y suficiente para el caso de uso
("cuando el empleado abre sesión en el navegador desde la sucursal") es el
controlador `web.Home.web_login`, heredado — se ejecuta una sola vez por
login interactivo, después de que Odoo ya confirmó la sesión (`request.session.uid`
seteado). / Overriding `res.users.authenticate` is fragile: Odoo 19 has
multiple auth paths (password, OAuth/SSO, 2FA, API keys) and not all of them
go through the same method. The stable, sufficient interception point for
this use case ("when the employee opens a browser session from the branch")
is the inherited `web.Home.web_login` controller — it runs exactly once per
interactive login, after Odoo has already confirmed the session
(`request.session.uid` set).

```python
# custom_addons/hr_attendance_ip_autologin/controllers/main.py
import logging

from odoo import http
from odoo.http import request
from odoo.addons.web.controllers.home import Home

_logger = logging.getLogger(__name__)


class HomeIPAutoCheckin(Home):

    @http.route()
    def web_login(self, redirect=None, **kw):
        response = super().web_login(redirect=redirect, **kw)

        # Solo actuar si el login fue exitoso (uid seteado en la sesion).
        # Only act if the login actually succeeded (uid set on the session).
        if request.session.uid:
            try:
                request.env["hr.employee"].sudo()._auto_checkin_by_ip(
                    request.session.uid, request.httprequest.remote_addr
                )
            except Exception:  # nunca bloquear el login por esto / never block login over this
                _logger.exception("hr_attendance_ip_autologin: fallo el intento de auto check-in")

        return response
```

**Diferencia con el código real / Difference from the real code:** la
condición final usa `request.params.get("login_success") and request.session.uid`,
no solo `request.session.uid` -- distingue un login real (POST recien
autenticado) de una simple recarga de `/web/login` con una sesión ya
existente, que también deja `session.uid` seteado. / The final condition
uses `request.params.get("login_success") and request.session.uid`, not
just `request.session.uid` -- it distinguishes a real login (freshly
authenticated POST) from a plain reload of `/web/login` with an
already-existing session, which also leaves `session.uid` set.

### Lógica de negocio / Business logic

```python
# custom_addons/hr_attendance_ip_autologin/models/hr_employee.py
from odoo import api, models


class HrEmployee(models.Model):
    _inherit = "hr.employee"

    @api.model
    def _auto_checkin_by_ip(self, user_id, remote_ip):
        employee = self.search([("user_id", "=", user_id)], limit=1)
        if not employee or not employee.company_id:
            return False

        authorized_ips = employee.company_id.attendance_authorized_ip_ids.filtered("active")
        if not any(rec._matches(remote_ip) for rec in authorized_ips):
            return False

        # No duplicar: solo marcar entrada si el empleado esta actualmente
        # afuera (sin un check-in abierto).
        # Don't duplicate: only mark check-in if the employee currently has
        # no open attendance.
        if employee.attendance_state == "checked_out":
            employee._attendance_action_change()
            return True
        return False
```

**Diferencia con el código real / Difference from the real code:** el
código real recorre TODOS los `hr.employee` del usuario (`search(...)` sin
`limit=1`), no solo el primero -- un mismo `res.users` puede tener un
`hr.employee` por cada compañía (confirmado: el usuario Administrator tiene
6), y había que hacer check-in en el que coincidiera con la sucursal de la
IP, no asumir que el primer resultado era el correcto. Este mismo patrón
multi-compañía se reutilizó para el checkout (ver abajo). / The real code
loops over ALL of the user's `hr.employee` records (`search(...)` without
`limit=1`), not just the first -- a single `res.users` can have one
`hr.employee` per company (confirmed: the Administrator user has 6), and
check-in had to happen on whichever one matched the IP's branch, not assume
the first search result was correct. This same multi-company pattern was
reused for checkout (see below).

### Riesgos y decisiones abiertas / Risks and open decisions

- **Falsos positivos en redes compartidas / False positives on shared
  networks:** si una sucursal usa NAT con una sola IP pública, cualquier
  dispositivo en esa red (celular personal por WiFi de invitados, por
  ejemplo) dispararía el check-in al hacer login. Mitigación propuesta:
  restringir el auto check-in a rangos CIDR internos específicos (LAN de
  oficina), no a la IP pública completa, y dejarlo como algo que el
  encargado de sucursal configura explícitamente. / If a branch uses NAT
  with a single public IP, any device on that network (a personal phone on
  guest WiFi, say) would trigger check-in on login. Proposed mitigation:
  restrict auto check-in to specific internal CIDR ranges (office LAN), not
  the whole public IP, left as something the branch manager configures
  explicitly.
- **No reemplaza el Quiosco / Does not replace the Kiosk:** este mecanismo es
  un complemento para asistencia vía backend (`attendance_from_systray`),
  no un reemplazo del modo Quiosco existente — ambos pueden convivir.
- **Extensión futura a almacén (fuera de alcance aquí) / Future extension to
  warehouse (out of scope here):** si más adelante se quisiera reusar este
  mismo patrón de IPs-por-sucursal para restringir *operaciones de almacén*
  (por ejemplo, permitir ajustes de cantidad **a la mano** solo desde la IP
  de la sucursal correspondiente), el modelo `hr.attendance.authorized.ip`
  se podría generalizar a `res.company` en vez de vivir en este módulo de
  RRHH — pero eso es una decisión de diseño separada, no parte de este
  entregable. / If this same per-branch IP pattern were later reused to
  restrict *warehouse operations* (e.g. only allowing **on hand** quantity
  adjustments from the matching branch's IP), the
  `hr.attendance.authorized.ip` model could be generalized off `res.company`
  instead of living in this HR module — but that's a separate design
  decision, not part of this deliverable.

### Vista de administración (implementada) / Admin view (implemented)

Vista de lista/form para `hr.attendance.authorized.ip` embebida como
pestaña "Check-in por IP" en el form de `res.company`
(`views/res_company_views.xml`), más un menú "IPs autorizadas" bajo
Asistencias > Configuración (`views/hr_attendance_authorized_ip_views.xml`).
ACL en `security/ir.model.access.csv` restringido a
`hr_attendance.group_hr_attendance_manager` para editar. Capturas:
`screenshots/authorized_ip_list.png`, `screenshots/company_ip_tab.png`. /
List/form view for `hr.attendance.authorized.ip` embedded as a "Check-in por
IP" tab on the `res.company` form (`views/res_company_views.xml`), plus an
"IPs autorizadas" menu under Attendances > Configuration
(`views/hr_attendance_authorized_ip_views.xml`). ACL in
`security/ir.model.access.csv` restricted to
`hr_attendance.group_hr_attendance_manager` for editing. Screenshots:
`screenshots/authorized_ip_list.png`, `screenshots/company_ip_tab.png`.

## Checkout dinámico (implementado) / Dynamic checkout (implemented)

Complementa el check-in por IP de arriba: cierra el turno del empleado sin
esperar a que alguien lo haga manualmente, cubriendo los dos escenarios de
"el empleado ya no está usando el backend" -- cerró sesión explícitamente, o
simplemente cerró el navegador. / Complements the IP check-in above: closes
the employee's shift without waiting for someone to do it by hand, covering
both scenarios of "the employee is no longer using the backend" -- they
logged out explicitly, or they just closed the browser.

### Logout explícito / Explicit logout

`controllers/main.py`, clase `SessionAutoCheckout`, override de
`odoo.addons.web.controllers.session.Session.logout`. Dos hallazgos que
cambiaron el diseño original respecto a lo que se había planteado
(interceptar `/web/session/destroy`):

- El botón "Cerrar sesión" del menú de usuario navega a
  **`/web/session/logout`** (`type='http'`), NO a `/web/session/destroy`
  (`type='jsonrpc'`, usado por otros flujos como `auth_timeout`) --
  confirmado leyendo `web/static/src/webclient/user_menu/user_menu_items.js`.
- `Session.logout()` llama a `request.session.clear()` **antes** del hook
  oficial `ir.http._post_logout()` -- para cuando ese hook corre,
  `request.env` ya es el usuario público. Por eso el checkout se captura
  leyendo `request.session.uid` **antes** de llamar a `super().logout()`,
  al revés de como `HomeIPAutoCheckin.web_login` lee el `uid` (ahí se lee
  *después* del `super()`, porque el login lo establece).

`models/hr_employee.py`, método `_auto_checkout_by_session(user_id)`: mismo
patrón multi-compañía que `_auto_checkin_by_ip` (recorre TODOS los
`hr.employee` del usuario). Nota: `attendance_state` es un campo `compute`
**sin `store=True`** -- no se puede filtrar en el dominio de `search()`
(Odoo lanza `ValueError: Cannot convert ... to SQL because it is not
stored`), así que el estado se filtra en Python, no en el dominio.

### Sesión abandonada / Abandoned session

Cron `ir_cron_auto_checkout_abandoned_sessions` (`data/ir_cron.xml`, cada 30
min) → `models/hr_employee.py`, método
`_cron_auto_checkout_abandoned_sessions()`. Hallazgo que cambió el diseño
original (que planteaba usar `ir.sessions`): **ese modelo no existe** en
esta instancia de Odoo 19 CE (verificado). La señal de "última actividad"
usada en su lugar es **`mail.presence.last_poll`** (módulo `mail`, agregado
como dependencia del manifest), actualizada por el heartbeat del *bus
longpolling* del cliente web mientras la pestaña del empleado sigue
abierta.

Reglas del cron:
- Umbral configurable vía `ir.config_parameter`
  `hr_attendance_ip_autologin.abandoned_session_timeout_hours` (default 8h)
  -- decisión deliberada de no exponerlo como campo en Ajustes hasta que
  haya una necesidad real de turnos de distinta duración por sucursal.
- Se excluyen a propósito los `hr.employee` **sin `user_id`** (empleados que
  marcan solo por Quiosco/código de barras, sin sesión de backend/bus) --
  cerrarlos sería un falso positivo, del mismo tipo que el ya documentado
  arriba para el check-in en redes NAT compartidas.
- Un empleado sin ningún `mail.presence` (nunca hubo heartbeat) se deja
  **sin tocar** en vez de asumir abandono -- mismo criterio conservador.
- `check_out` se fija en `presence.last_poll`, no en "ahora", para que las
  horas contabilizadas se acerquen al momento real en que el navegador dejó
  de responder.
- Si `last_poll` no es **posterior** a `check_in` (señal inútil para ese
  registro puntual) o el `write()` falla por cualquier otra razón (p. ej.
  Odoo rechaza un `check_out` anterior al `check_in`), ese registro se omite
  con un log de advertencia **sin abortar el resto del lote** -- un
  problema puntual no debe impedir que se cierren las demás sesiones
  abandonadas de esa corrida.
- `ir.cron` en Odoo 19 ya no tiene el campo `numbercall` (los cron corren
  indefinidamente, controlados solo por `active`) -- diferencia respecto a
  versiones anteriores de Odoo a tener en cuenta si se reusa este patrón en
  otro módulo.

Verificado end-to-end con Playwright (`e2e_tests/test_attendance_checkout.spec.ts`):
login real, logout real, y disparo manual del cron vía
`ir.cron.method_direct_trigger` (el mismo método que usa el botón "Run
Manually" del form de `ir.cron`).
