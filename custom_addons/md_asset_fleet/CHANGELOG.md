# Changelog — md_asset_fleet

## 2026-08-13

- ✨ feat(md_asset_fleet): módulo nuevo — extiende `fleet.vehicle` con vínculo a `hr.employee` (`employee_id`), sincronizado con `driver_id`. La multi-sucursal usa el `company_id` nativo de fleet (6 sucursales de Medicine Depot: Campeche, Cancún, Chetumal, Playa del Carmen, Ticul, Mérida).
