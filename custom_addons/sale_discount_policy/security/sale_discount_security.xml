<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sale_discount_policy_comp_rule" model="ir.rule">
        <field name="name">Sale Discount Policy: company</field>
        <field name="model_id" ref="model_sale_discount_policy"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>
</odoo>
