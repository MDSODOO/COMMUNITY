<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">
        <record id="action_clean_archived_lots_company" model="ir.actions.server">
            <field name="name">Limpiar empresa en lotes archivados</field>
            <field name="model_id" ref="stock.model_stock_lot"/>
            <field name="binding_model_id" ref="stock.model_stock_lot"/>
            <field name="binding_type">action</field>
            <field name="state">code</field>
            <field name="code">action = model.action_clean_archived_company()</field>
        </record>
    </data>
</odoo>
