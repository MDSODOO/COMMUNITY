# purchase_lot_selection — v19.0.99.0.0 (Bridge)

**Categoría**: Tools | **Tipo**: Módulo bridge de compatibilidad

## Propósito

Alias de compatibilidad hacia `lot_selection`. Instalaciones antiguas que dependían de `purchase_lot_selection` siguen funcionando sin cambios de configuración.

**Toda la lógica real está en [`lot_selection`](../lot_selection/README.md).**

```python
depends = ['lot_selection']
```

La versión `99` indica que este es un módulo bridge permanente — no se espera evolución funcional.

[⏳ MÓDULO(S) ACTUALIZADO(S)/AUDITADO(S) EN ESTE PASO: purchase_lot_selection]
