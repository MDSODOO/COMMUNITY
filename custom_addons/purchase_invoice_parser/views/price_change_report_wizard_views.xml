<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ===================================================================== -->
    <!-- WIZARD: Reporte de Costos por Sucursal                               -->
    <!-- ===================================================================== -->
    <record id="view_price_change_report_wizard_form" model="ir.ui.view">
        <field name="name">purchase.price.change.report.wizard.form</field>
        <field name="model">purchase.price.change.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Reporte de Costos por Sucursal">
                <group>
                    <group string="Filtros">
                        <field name="line_ids" widget="many2many_tags"
                               string="Línea de Producto"
                               placeholder="Vacío = todas las líneas"/>
                        <field name="company_ids" widget="many2many_tags"
                               string="Sucursales"/>
                    </group>
                </group>
                <footer>
                    <button string="Generar PDF"
                            name="action_print_report"
                            type="object"
                            class="btn-primary"
                            icon="fa-file-pdf-o"/>
                    <button string="Exportar Excel"
                            name="action_export_excel"
                            type="object"
                            class="btn-secondary"
                            icon="fa-file-excel-o"/>
                    <button string="Cancelar"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_cost_by_branch_wizard" model="ir.actions.act_window">
        <field name="name">Costos por Sucursal</field>
        <field name="res_model">purchase.price.change.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
